# Archive

Earlier versions of the site, kept for reference. Nothing here is linked from the
live page and nothing here should be edited.

- `phase1-launch.html` — the first public page: centered logo with links to Spotify,
  Apple Podcasts and Deezer. Platform icons are inlined as base64, so the file is
  self contained.
